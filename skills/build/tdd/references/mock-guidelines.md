# 模拟对象规则

判断要不要 mock 时读取本文件。

## 优先用真实代码

依赖如果快速、确定、在本地运行，默认用真实实现。

## 适合使用模拟对象的情况

- 外部网络或付费 API。
- 慢或不稳定的依赖。
- 时间、随机数等非确定行为，且无法注入。
- 真实触发失败路径不安全或不可行。

## 避免

- 因为容易 mock 就 mock。
- 断言内部调用顺序，除非顺序是 public behavior。
- mock 里复制实现逻辑。

## 更好的接口

mock 很痛苦时，通常是接口不对。考虑用 `refactor` 建 seam。
