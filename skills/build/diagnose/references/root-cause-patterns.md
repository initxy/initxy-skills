# 根因定位模式

诊断卡住时读取本文件。

## 反向追踪

1. 找到坏值被观察到的位置。
2. 找到是谁产出了这个值。
3. 持续往上游追，直到找到第一个错误转换。
4. 修源头，不修症状。

## 边界打点

多组件 bug 要在每个边界记录：

- 进入边界的输入。
- 离开边界的输出。
- 该边界可见的配置/环境。
- 关联 ID。

最终交付前删除临时日志。

## 三次修复失败

如果三次修复都失败，停止改代码，重建假设：

- 架构是否错了？
- 复现是否错了？
- 观察到的失败是否只是另一层的症状？
- 是否有状态、缓存或测试污染？
